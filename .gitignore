# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*

node_modules
dist
dist-ssr
*.local
.coverage

# Brand-specific env overrides for downstream consumers (e.g. S2GOS) are not
# part of this generic library; consumers create these locally from their
# own documentation and never commit them here.
# TODO: remove me, I don't belong here!
.env.s2gos

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# vitest / playwright
__screenshots__
